{
  "name": "texture-renderer",
  "version": "0.1.0",
  "main": "index.js",
  "scripts": {
    "build": "napi build --platform --release",
    "build-debug": "napi build --platform"
  },
  "devDependencies": {
    "@napi-rs/cli": "^2.18.0"
  },
  "napi": {
    "name": "texture-renderer",
    "triples": {
      "defaults": true
    }
  }
}
