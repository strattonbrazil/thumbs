{
  "name": "electron-rust-texture",
  "version": "1.0.0",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build-native": "cd native && npm run build && cd .."
  },
  "devDependencies": {
    "electron": "^28.0.0"
  }
}
